# Hospital Project

Monorepo containing:

- `Frontend/` (Vite + React)
- `Backend/` (Node/Express)

## Quick start

Frontend:

```bash
cd Frontend
npm install
npm run dev
```

Backend:

```bash
cd Backend
npm install
npm run dev
```

